\matrix [fptk, list=a] {
    \elem{1} &
    \elem{2} &
    \elems &
    \elem{i} &
    \elemsafter \\
};

\matrix [fptk, list=b, element=a, below=2 of a.south west, anchor=north west] {
    \elem{1} &
    \elemx{elem1}{\texttt{elem}} &
    \elem{2} &
    \elemx{elem2}{\texttt{elem}} &
    \elems &
    \elemx{elemi-1}{\texttt{elem}} &
    \elem{i} &
    \elemx{elemi}{\texttt{elem}} &
    \elemsafter \\
};

\foreach \i in {1,2,i}{
    \draw [fptk, flow ->=zigzag] (a\i.south) -- (b\i.north);
}

\foreach \i in {1,2,i-1,i}{
    \draw [fptk, <- flow] (belem\i.north) -- +(0, .5);
}
