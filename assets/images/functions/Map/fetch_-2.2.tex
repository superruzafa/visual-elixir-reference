\matrix [fptk, map=a] {
    \keyvalue{1} &
    \keyvalue{2} &
    \elems &
    \keyvalue{n} \\
};

\iterateto{a1.north}
\iteratefromto{a1.north}{a2.north}
\iteratefrom{a2.north}
\iterateto{an.north}

\foreach \i/\d in {
    1/.5,
    2/1,
    n/.5
}{
    \predtop[\d]{a\i.north}{$k_\i\ne\texttt{key}$}
}

\node (b) [fptk, exception=KeyError, below right=0 and 1 of a, anchor=north];
\draw [fptk, throw ->] (an.north) .. controls +(1, 1) and +(0, 2) .. (b);
