\matrix [list=a] {
    \node (a) {$\{a_1,\ldots,a_{\small\texttt{index}},\ldots,a_n\}$}; &
    \node (b) {$\{b_1,\ldots,b_{\small\texttt{index}},\ldots,b_n\}$}; &
    \node [elements between]; &
    \node (k) {$\{k_1,\ldots,\texttt{key},\ldots,k_n\}$}; &
    \node [elements after]; \\
};

\draw [enter iteration] (a.north);
\draw [iteration] (a.north) -- (b.north);
\draw [iteration=dashed] (b.north) -- (k.north);

\draw [->] (k.south) -- +(0, -1)
    node [below] {$\{k_1,\ldots,\texttt{key},\ldots,k_n\}$};
