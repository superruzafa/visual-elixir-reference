\matrix [list=a] {
    \node (a) {$\{a_1,\ldots,a_{\small\texttt{index}},\ldots,a_n\}$}; &
    \node [elements between]; &
    \node (z) {$\{z_1,\ldots,z_{\small\texttt{index}},\ldots,z_n\}$}; \\
};

\draw [enter iteration] (a.north);
\draw [iteration=dashed] (a.north) -- (z.north);

\node (b) [below right=1 and 1 of list a, anchor=north] {\nil};
\draw [->] (z.north) .. controls +(1, 1) and +(0, 3) .. (b);
