\matrix [list=a] {
  \node [index=1]; &
  \node [index=2]; &
  \node [elements between]; &
  \node [index=n]; \\
};

\foreach \i in {1, 2, n}{
  \draw [->] (a\i.south) -- +(0, -.5)
    node (fun \i) [fun, anchor=in];
  \draw [->] (fun \i.out) -- +(0, -.5)
    node [below] (w\i) {$w_\i$};
}

\coordinate (x) at ($ (w1.south)!.5!(wn.south) $);

\node (b) [below=2 of x] {\{$a_{min}$,\,$a_{max}$\}};

\node at ($ (x)!.5!(b.north) $) {$\Downarrow$};

\matrix [
  matrix of math nodes,
  nodes={},
  row sep=1, 
  left delimiter=\{,
  right=of b
] {
  a_{min} \leq \texttt{fun}(a_i),\, i \in [1, n] \\
  a_{max} \geq \texttt{fun}(a_i),\, i \in [1, n] \\
};
