\matrix [fptk, list=a] {
    \elem{1} &
    \elem{2} &
    \elems &
    \elem{n} \\
};

\node (b) [below=2 of a] {\{$a_{min}$,\,$a_{max}$\}};

\bigarrowdown{$ (a.south)!.5!(b.north) $}

\matrix [
  matrix of math nodes,
  nodes={inner ysep=.125\masterunit},
  row sep=.125\masterunit, 
  left delimiter=\{,
  right=.5 of b
] {
  a_{min} \leq a_i,\, i \in [1, n] \\
  a_{max} \geq a_i,\, i \in [1, n] \\
};
