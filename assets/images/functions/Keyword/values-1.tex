\matrix [fptk, keyword list=a] {
    \keyword{1} &
    \keyword{2} &
    \elems &
    \keyword{n} \\
};

\matrix [fptk, list=b, below=2 of a] {
    \elem[k]{1} &
    \elem[k]{2} &
    \elems &
    \elem[k]{n} \\
};

\foreach \i in {1,2,n}{
    \draw [fptk, flow] ($ (a\i.south) + (.25, .25) $) -- (\currcoord |- a\i.south) coordinate (x);
    \draw [fptk, flow ->=soft] (x) -- (b\i.north);
}