\begin{document}
\begin{tikzpicture}[use funprog]

\tikzset{
    x=1.3cm,
    y=1.3cm,
}

\tikzset{
    ->/.style={
        line width=0.4mm,
        -{Triangle[angle=37.5:2.5mm]}
    },
    every list/.style={
        line width=0.8mm,
        nodes={
            font=\Large,
            minimum size=1.3cm,
        }
    },
    every list elements between/.style={
        minimum width=1.5cm,
    },
    every function/.style={
        line width=0.4mm,
        font=\large,
    }
}

\def\true{$\texttt{true}$}
\def\false{$\texttt{false}$}
\def\nil{$\texttt{nil}$}

\newcommand\applyfunabove[3]{
    \node (last value) [above=2 of #1] {#3};
    \node (last function) at ($ (#1)!.5!(last value) $) [function, in=south, out=north] {#2};
    \draw [->] (#1) -- (last function.in);
    \draw [->] (last function.out) -- (last value);
}

