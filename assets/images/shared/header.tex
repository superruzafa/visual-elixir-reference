%

\begin{document}
\begin{tikzpicture}[use funprog]

\tikzset{
	x=1.25cm,
	y=1.25cm,
	every list/.style={
		line width=0.8mm,
		font=\Large,
		minimum size=1.25cm,
	},
	every list elements between/.style={
		minimum width=.75cm,
	},
	every function/.style={
		line width=0.4mm,
		font=\large,
	},
	->/.style={
		-Stealth,
		line width=0.4mm,
	}
}

\def\true{\texttt{true}}
\def\false{\texttt{false}}
\def\nil{\texttt{nil}}

\newcommand\predicateabove[4][2]{
    \node (last label) at ($ (#2) + (0, #1) $) {#4};
    \node at ($ (#2)!.5!($ (#2) + (0, #1) $) $) (last function) [funprog/function/in=south, funprog/function/out=north, function=fun] {#3};
    \draw [->] (#2) -- (function fun.in);
    \draw [->, shorten >=1mm] (function fun.out) -- (last label.base);
}

