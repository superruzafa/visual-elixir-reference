- name: Atom
  functions:
    - name: to_charlist/1
      signature: to_charlist(atom)
    - name: to_string/1
      signature: to_string(atom)

- name: Enum
  functions:
    - name: all?/1
      signature: all?(enumerable)
    - name: all?/2
      signature: all?(enumerable, fun)
    - name: any?/1
      signature: any?(enumerable)
    - name: any?/2
      signature: any?(enumerable, fun)
    - name: at/3
      signature: at(enumerable, index, default \\ nil)
    - name: chunk_by/2
      signature: chunk_by(enumerable, fun)
    - name: chunk_every/2
      signature: chunk_every(enumerable, count)
    - name: chunk_every/4
      signature: chunk_every(enumerable, count, step, leftover \\ [])
    - name: chunk_while/4
      signature: chunk_while(enumerable, acc, fun, after_fun)
    - name: concat/1
      signature: concat(enumerables)
    - name: concat/2
      signature: concat(left, right)
    - name: count/1
      signature: count(enumerable)
    - name: count/2
      signature: count(enumerable, fun)
    - name: count_until/2
      signature: count_until(enumerable, limit)
    - name: count_until/3
      signature: count_until(enumerable, fun, limit)
    - name: dedup/1
      signature: dedup(enumerable)
    - name: dedup_by/2
      signature: dedup_by(enumerable, fun)
    - name: drop/2
      signature: drop(enumerable, count)
    - name: drop_every/2
      signature: drop_every(enumerable, nth)
    - name: drop_while/2
      signature: drop_while(enumerable, fun)
    - name: each/2
      signature: each(enumerable, fun)
    - name: empty?/1
      signature: empty?(enumerable)
    - name: fetch/2
      signature: fetch(enumerable, index)
    - name: fetch!/2
      signature: fetch!(enumerable, index)
    - name: filter/2
      signature: filter(enumerable, fun)
    - name: find/3
      signature: find(enumerable, default \\ nil, fun)
    - name: find_index/2
      signature: find_index(enumerable, fun)
    - name: find_value/3
      signature: find_value(enumerable, default \\ nil, fun)
    - name: flat_map/2
      signature: flat_map(enumerable, fun)
    - name: flat_map_reduce/3
      signature: flat_map_reduce(enumerable, acc, fun)
    - name: frequencies/1
      signature: frequencies(enumerable)
    - name: frequencies_by/2
      signature: frequencies_by(enumerable, fun)
    - name: group_by/3
      signature: group_by(enumerable, key_fun, val_fun \\ fn x -> x end)
    - name: intersperse/2
      signature: intersperse(enumerable, elem)
    - name: join/2
      signature: join(enumerable, joiner \\ "")
    - name: map/2
      signature: map(enumerable, fun)
    - name: map_every/3
      signature: map_every(enumerable, nth, fun)
    - name: map_intersperse/3
      signature: map_intersperse(enumerable, elem, fun)
    - name: map_join/3
      signature: map_join(enumerable, joiner \\ "", fun)
    - name: map_reduce/3
      signature: map_reduce(enumerable, acc, fun)
    - name: max/3
      signature: max(enumerable, sort \\ &>=/2)
    - name: max_by/4
      signature: max_by(enumerable, fun, sort \\ &>=/2)
    - name: member?/2
      signature: member?(enumerable, elem)
    - name: min/3
      signature: min(enumerable, sort \\ &<=/2)
    - name: min_by/4
      signature: min_by(enumerable, fun, sort \\ &<=/2)
    - name: min_max/2
      signature: min_max(enumerable)
    - name: min_max_by/3
      signature: min_max_by(enumerable, fun, sorter_or_empty_fallback \\ &</2)
    - name: product/1
      signature: product(enumerable)
    - name: product_by/2
      signature: product_by(enumerable, fun)
    - name: random/1
      signature: random(enumerable)
    - name: reduce/2
      signature: reduce(enumerable, fun)
    - name: reduce/3
      signature: reduce(enumerable, acc, fun)
    - name: reduce_while/3
      signature: reduce_while(enumerable, acc, fun)
    - name: reject/2
      signature: reject(enumerable, fun)
    - name: reverse/1
      signature: reverse(enumerable)
    - name: reverse/2
      signature: reverse(enumerable, tail)
    - name: reverse_slice/3
      signature: reverse_slice(enumerable, start, count)
    - name: scan/2
      signature: scan(enumerable, fun)
    - name: scan/3
      signature: scan(enumerable, acc, fun)
    - name: shuffle/1
      signature: shuffle(enumerable)
    - name: slice/2
      signature: slice(enumerable, range)
    - name: slice/3
      signature: slice(enumerable, start, count)
    - name: slide/3
      signature: slide(enumerable, src, dst)
    - name: sort/1
      signature: sort(enumerable)
    - name: sort/2
      signature: sort(enumerable, fun)
    - name: sort_by/3
      signature: sort_by(enumerable, mapper, sorter \\ &<=/2)
    - name: split/2
      signature: split(enumerable, count)
    - name: split_while/2
      signature: split_while(enumerable, fun)
    - name: split_with/2
      signature: split_with(enumerable, fun)
    - name: sum/1
      signature: sum(enumerable)
    - name: sum_by/2
      signature: sum_by(enumerable, fun)
    - name: take/2
      signature: take(enumerable, count)
    - name: take_every/2
      signature: take_every(enumerable, nth)
    - name: take_random/2
      signature: take_random(enumerable, count)
    - name: take_while/2
      signature: take_while(enumerable, fun)
    - name: uniq/1
      signature: uniq(enumerable)
    - name: uniq_by/2
      signature: uniq_by(enumerable, fun)
    - name: unzip/1
      signature: unzip(enumerable)
    - name: with_index/2
      signature: with_index(enumerable, offset \\ 0)
    - name: zip/1
      signature: zip(enumerables)
    - name: zip/2
      signature: zip(enumerable1, enumerable2)
    - name: zip_reduce/3
      signature: zip_reduce(enumerables, acc, fun)
    - name: zip_reduce/4
      signature: zip_reduce(left, right, acc, fun)
    - name: zip_with/2
      signature: zip_with(enumerables, fun)
    - name: zip_with/3
      signature: zip_with(enumerable1, enumerable2, fun)

- name: Integer
  functions:
    - name: digits/2
      signature: digits(integer, base \\ 10)
    - name: to_string/2
      signature: to_string(integer, base \\ 10)
    - name: undigits/2
      signature: undigits(digits, base \\ 10)

- name: List
  functions:
    - name: delete/2
      signature: delete(list, elem)
    - name: delete_at/2
      signature: delete_at(list, index)
    - name: duplicate/2
      signature: duplicate(elem, count)
    - name: first/2
      signature: first(list, default \\ nil)
    - name: flatten/1
      signature: flatten(list)
    - name: flatten/2
      signature: flatten(list, tail)
    - name: foldl/3
      signature: foldl(list, acc, fun)
    - name: foldr/3
      signature: foldr(list, acc, fun)
    - name: insert_at/3
      signature: insert_at(list, index, value)
    - name: keydelete/3
      signature: keydelete(list, key, index)
    - name: keyfind/4
      signature: keyfind(list, key, index, default \\ nil)
    - name: keymember?/3
      signature: keymember?(list, key, index)
    - name: keyreplace/4
      signature: keyreplace(list, key, index, new_tuple)
    - name: keystore/4
      signature: keystore(list, key, index, new_tuple)
    - name: keytake/3
      signature: keytake(list, key, index)
    - name: last/2
      signature: last(list, default \\ nil)
    - name: pop_at/3
      signature: pop_at(list, index, default \\ nil)
    - name: replace_at/3
      signature: replace_at(list, index, value)
    - name: starts_with?/2
      signature: starts_with?(list, prefix)
    - name: to_integer/1
      signature: to_integer(charlist)
    - name: to_integer/2
      signature: to_integer(charlist, base)
    - name: to_tuple/1
      signature: to_tuple(list)
    - name: update_at/3
      signature: update_at(list, index, fun)
    - name: wrap/1
      signature: wrap(term)

- name: Kernel
  functions:
    - name: elem/2
      signature: elem(tuple, index)
    - name: put_elem/3
      signature: put_elem(tuple, index, value)
    - name: tap/2
      signature: tap(value, fun)
    - name: then/2
      signature: then(value, fun)

- name: Keyword
  functions:
    - name: delete/2
      signature: delete(keywords, key)
    - name: delete_first/2
      signature: delete_first(keywords, key)
    - name: drop/2
      signature: drop(keywords, keys)
    - name: fetch/2
      signature: fetch(keywords, key)
    - name: fetch!/2
      signature: fetch!(keywords, key)
    - name: filter/2
      signature: filter(keywords, fun)
    - name: from_keys/2
      signature: from_keys(keys, value)
    - name: get/3
      signature: get(keywords, key, default \\ nil)
    - name: get_lazy/3
      signature: get_lazy(keywords, key, fun)
    - name: get_values/2
      signature: get_values(keywords, key)
    - name: has_key?/2
      signature: has_key?(keywords, key)
    - name: keys/1
      signature: keys(keywords)
    - name: keyword?/1
      signature: keyword?(term)
    - name: new/0
      signature: new()
    - name: new/1
      signature: new(enumerable)
    - name: new/2
      signature: new(enumerable, fun)
    - name: pop/3
      signature: pop(keywords, key, default \\ nil)
    - name: pop!/2
      signature: pop!(keywords, key)
    - name: pop_first/3
      signature: pop_first(keywords, key, default \\ nil)
    - name: pop_lazy/3
      signature: pop_lazy(keywords, key, fun)
    - name: pop_values/2
      signature: pop_values(keywords, key)
    - name: put/3
      signature: put(keywords, key, value)
    - name: put_new/3
      signature: put_new(keywords, key, value)
    - name: put_new_lazy/3
      signature: put_new_lazy(keywords, key, fun)
    - name: reject/2
      signature: reject(keywords, fun)
    - name: replace/3
      signature: replace(keywords, key, value)
    - name: replace!/3
      signature: replace!(keywords, key, value)
    - name: replace_lazy/3
      signature: replace_lazy(keywords, key, fun)
    - name: split/2
      signature: split(keywords, keys)
    - name: take/2
      signature: take(keywords, keys)
    - name: to_list/1
      signature: to_list(keywords)
    - name: update/4
      signature: update(keywords, key, value, fun)
    - name: update!/3
      signature: update!(keywords, key, fun)
    - name: values/1
      signature: values(keywords)

- name: Map
  functions:
    - name: delete/2
      signature: delete(map, key)
    - name: drop/2
      signature: drop(map, keys)
    - name: fetch/2
      signature: fetch(map, key)
    - name: fetch!/2
      signature: fetch!(map, key)
    - name: get/3
      signature: get(map, key, default \\ nil)
    - name: get_and_update/3
      signature: get_and_update(map, key, fun)
    - name: get_and_update!/3
      signature: get_and_update!(map, key, fun)
    - name: get_lazy/3
      signature: get_lazy(map, key, fun)
    - name: has_key?/2
      signature: has_key?(map, key)
    - name: keys/1
      signature: keys(map)
    - name: merge/2
      signature: merge(map1, map2)
    - name: merge/3
      signature: merge(map1, map2, fun)
    - name: new/0
      signature: new()
    - name: new/1
      signature: new(enumerable)
    - name: new/2
      signature: new(enumerable, fun)
    - name: pop!/2
      signature: pop!(map, key)
    - name: pop/3
      signature: pop(map, key, default \\ nil)
    - name: pop_lazy/3
      signature: pop_lazy(map, key, fun)
    - name: put/3
      signature: put(map, key, value)
    - name: put_new/3
      signature: put_new(map, key, value)
    - name: put_new_lazy/3
      signature: put_new_lazy(map, key, fun)
    - name: replace!/3
      signature: replace!(map, key, value)
    - name: split/2
      signature: split(map, keys)
    - name: take/2
      signature: take(map, keys)
    - name: to_list/1
      signature: to_list(map)
    - name: update/4
      signature: update(map, key, value, fun)
    - name: update!/3
      signature: update!(map, key, fun)
    - name: values/1
      signature: values(map)

- name: MapSet
  functions:
    - name: difference/2
      signature: difference(map_set1, map_set2)
    - name: filter/2
      signature: filter(map_set, fun)
    - name: intersection/2
      signature: intersection(map_set1, map_set2)
    - name: new/0
      signature: new()
    - name: new/1
      signature: new(enumerable)
    - name: new/2
      signature: new(enumerable, fun)
    - name: reject/2
      signature: reject(map_set, fun)
    - name: to_list/1
      signature: to_list(map_set)
    - name: size/1
      signature: size(map_set)
    - name: split_with/2
      signature: split_with(map_set, fun)
    - name: union/2
      signature: union(map_set1, map_set2)

- name: Range
  functions:
    - name: disjoint?/2
      signature: disjoint?(range1, range2)
    - name: new/2
      signature: new(first, last)
    - name: new/3
      signature: new(first, last, step)
    - name: to_list/1
      signature: to_list(range)

- name: Stream
  functions:
    - name: chunk_by/2
      signature: chunk_by(stream, fun)
    - name: chunk_every/2
      signature: chunk_every(stream, count)
    - name: chunk_every/4
      signature: chunk_every(enum, count, step, leftover \\ [])
    - name: concat/1
      signature: concat(enumerables)
    - name: concat/2
      signature: concat(left, right)
    - name: chunk_while/4
      signature: chunk_while(stream, acc, fun, after_fun)
    - name: cycle/1
      signature: cycle(enumerable)
    - name: dedup/1
      signature: dedup(enumerable)
    - name: dedup_by/2
      signature: dedup_by(enumerable, fun)
    - name: drop/2
      signature: drop(enumerable, count)
    - name: drop_every/2
      signature: drop_every(enumerable, nth)
    - name: drop_while/2
      signature: drop_every(enumerable, fun)
    - name: duplicate/2
      signature: duplicate(elem, count)
    - name: each/2
      signature: each(enumerable, fun)
    - name: filter/2
      signature: filter(enumerable, fun)
    - name: flat_map/2
      signature: flat_map(enumerable, fun)
    - name: from_index/1
      signature: from_index(fun_or_offset \\ 0)
    - name: intersperse/2
      signature: intersperse(enumerable, elem)
    - name: interval/1
      signature: interval(n)
    - name: iterate/2
      signature: iterate(start, fun)
    - name: map/2
      signature: map(enumerable, fun)
    - name: map_every/3
      signature: map_every(enumerable, nth, fun)
    - name: reject/2
      signature: reject(enumerable, fun)
    - name: repeatedly/1
      signature: repeatedly(fun)
    - name: resource/3
      signature: resource(start_fun, fun, after_fun)
    - name: run/1
      signature: run(stream)
    - name: scan/2
      signature: scan(enumerable, fun)
    - name: scan/3
      signature: scan(enumerable, acc, fun)
    - name: take/2
      signature: take(enumerable, count)
    - name: take_while/2
      signature: take(enumerable, fun)
    - name: take_every/2
      signature: take_every(enumerable, count)
    - name: timer/1
      signature: timer(n)
    - name: transform/3
      signature: transform(enumerable, acc, fun)
    - name: transform/4
      signature: transform(enumerable, start_fun, fun, after_fun)
    - name: transform/5
      signature: transform(enumerable, start_fun, fun, last_fun, after_fun)
    - name: unfold/2
      signature: unfold(acc, fun)
    - name: uniq/1
      signature: uniq(enumerable)
    - name: uniq_by/2
      signature: uniq_by(enumerable, fun)
    - name: with_index/2
      signature: with_index(enumerable, offset \\ 0)
    - name: zip/1
      signature: zip(enumerable)
    - name: zip/2
      signature: zip(left, right)
    - name: zip_with/2
      signature: zip_with(enumerables, fun)
    - name: zip_with/3
      signature: zip_with(enumerable1, enumerable2, fun)

- name: Task
  functions:
    - name: async/1
      signature: async(fun)

- name: Tuple
  functions:
    - name: delete_at/2
      signature: delete_at(tuple, index)
    - name: duplicate/2
      signature: duplicate(value, size)
    - name: insert_at/3
      signature: insert_at(tuple, index, value)
    - name: product/1
      signature: product(tuple)
    - name: sum/1
      signature: sum(tuple)
    - name: to_list/1
      signature: to_list(tuple)
